# Minimal neovim configuration
Minimal nvim configuration, that should work out of the box and have no outside dependencies and super fast


# Requirements
Neovim >= 0.8.0 (needs to be built with LuaJIT)
Git >= 2.19.0 (for partial clones support)



